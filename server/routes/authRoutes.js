const express=require('express');const router=express.Router();const c=require('../controllers/authController');const auth=require('../middleware/auth');router.post('/send-otp',c.sendOtp);router.post('/verify-otp',c.verifyOtp);router.post('/employer/send-otp',c.sendEmployerOtp);router.post('/employer/verify-otp',c.verifyEmployerOtp);router.get('/me',auth,c.getMe);module.exports=router;
